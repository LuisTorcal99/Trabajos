<UserControl x:Class="ExamenFinal.View.PrincipalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ExamenFinal.View" 
             xmlns:viewmodel="clr-namespace:ExamenFinal.ViewModel" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml" 
             xmlns:controls="clr-namespace:ExamenFinal.Controls"
             d:DataContext="{d:DesignInstance Type=viewmodel:PrincipalViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="White">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="7.5*"/>
            <RowDefinition Height="2.5*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="5">
            <!--<ui:Button Content="AddObjeto" Margin="10" Command="{Binding AddObjetoCommand}" Background="#2C3E50" Foreground="White" FontSize="18" Width="140"/>
            <ui:Button Content="AddObjetoDos" Margin="10" Command="{Binding AddObjetoDosCommand}" Background="#2C3E50" Foreground="White" FontSize="18" Width="140"/>-->
            <ui:Button Content="AddPedido" Margin="10" Command="{Binding AddObjetoCommand}" Background="#2C3E50" Foreground="White" FontSize="18" Width="140"/>
            <Button Content="Importar" Width="120" Height="40" Margin="5" Command="{Binding ImportarCommand}" Background="DeepSkyBlue" Foreground="Black" FontFamily="Arial Black"/>
            <Button Content="Exportar" Width="120" Height="40" Margin="5" Command="{Binding ExportarCommand}" Background="MediumPurple" Foreground="Black" FontFamily="Arial Black"/>

        </StackPanel>

        <!-- Contenedor de objetos separados en tres StackPanel -->
        <!--<ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="20">-->
                <!-- Primer StackPanel para los objetos -->
        <StackPanel x:Name="StackPanelObjeto1" Margin="20">
            <ItemsControl ItemsSource="{Binding Objeto3Items}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Command="{Binding DataContext.ItemClickCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                        CommandParameter="{Binding Id}" >  
                                    <Border Margin="5" Padding="10" BorderThickness="1" BorderBrush="#BDC3C7" Background="#ECF0F1" CornerRadius="8">
                                        <StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="5">
                                                <Label Content="ID:" VerticalAlignment="Center" Width="40" HorizontalAlignment="Right" FontSize="10" Foreground="#34495E"/>
                                                <TextBlock Text="{Binding Id}" FontSize="16" FontWeight="Bold" VerticalAlignment="Center" Foreground="Black" Margin="10,0"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="5">
                                        <Label Content="Usuario:" VerticalAlignment="Center" Width="60" HorizontalAlignment="Right" FontSize="10" Foreground="#34495E"/>
                                                <TextBlock Text="{Binding Usuario}" FontSize="16" VerticalAlignment="Center" Foreground="Black" Margin="10,0"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="5">
                                        <Label Content="Productos:" VerticalAlignment="Center" Width="90" HorizontalAlignment="Right" FontSize="10" Foreground="#34495E"/>
                                                <TextBlock Text="{Binding Productos}" FontSize="16" VerticalAlignment="Center" Foreground="Black" Margin="10,0"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="5">
                                        <Label Content="Fecha:" VerticalAlignment="Center" Width="90" HorizontalAlignment="Right" FontSize="10" Foreground="#34495E"/>
                                                <TextBlock Text="{Binding Fecha}" FontSize="16" VerticalAlignment="Center" Foreground="Black" Margin="10,0"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>


                <!-- Segundo StackPanel para los objetos --><!--
                <StackPanel x:Name="StackPanelObjeto2" Margin="20">
                    <TextBlock Text="Objeto 2" FontWeight="Bold" FontSize="20" Margin="5"/>
                    <ItemsControl ItemsSource="{Binding Objeto2Items}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Margin="5" Padding="10" BorderThickness="1" BorderBrush="#BDC3C7" Background="#ECF0F1" CornerRadius="8">
                                    <StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="5">
                                            <Label Content="ID:" VerticalAlignment="Center" Width="40" HorizontalAlignment="Right" FontSize="10" Foreground="#34495E"/>
                                            <TextBlock Text="{Binding Id}" FontSize="16" FontWeight="Bold" VerticalAlignment="Center" Foreground="Black" Margin="10,0"/>
                                        </StackPanel>

                                        <StackPanel Orientation="Horizontal" Margin="5">
                                            <Label Content="Nombre:" VerticalAlignment="Center" Width="60" HorizontalAlignment="Right" FontSize="10" Foreground="#34495E"/>
                                            <TextBlock Text="{Binding Name}" FontSize="16" VerticalAlignment="Center" Foreground="Black" Margin="10,0"/>
                                        </StackPanel>

                                        <StackPanel Orientation="Horizontal" Margin="5">
                                            <Label Content="Objeto ID:" VerticalAlignment="Center" Width="60" HorizontalAlignment="Right" FontSize="10" Foreground="#34495E"/>
                                            <TextBlock Text="{Binding IdObjeto}" FontSize="16" VerticalAlignment="Center" Foreground="Black" Margin="10,0"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>

                --><!-- Tercer StackPanel para los objetos --><!--
                <StackPanel x:Name="StackPanelObjeto3" Margin="20">
                    <TextBlock Text="Objeto 3" FontWeight="Bold" FontSize="20" Margin="5"/>
                    <ItemsControl ItemsSource="{Binding Objeto3Items}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Margin="5" Padding="10" BorderThickness="1" BorderBrush="#BDC3C7" Background="#ECF0F1" CornerRadius="8">
                                    <StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="5">
                                            <Label Content="ID:" VerticalAlignment="Center" Width="40" HorizontalAlignment="Right" FontSize="10" Foreground="#34495E"/>
                                            <TextBlock Text="{Binding Id}" FontSize="16" FontWeight="Bold" VerticalAlignment="Center" Foreground="Black" Margin="10,0"/>
                                        </StackPanel>

                                        <StackPanel Orientation="Horizontal" Margin="5">
                                            <Label Content="Nombre:" VerticalAlignment="Center" Width="60" HorizontalAlignment="Right" FontSize="10" Foreground="#34495E"/>
                                            <TextBlock Text="{Binding Name}" FontSize="16" VerticalAlignment="Center" Foreground="Black" Margin="10,0"/>
                                        </StackPanel>

                                        <StackPanel Orientation="Horizontal" Margin="5">
                                            <Label Content="IdObjeto:" VerticalAlignment="Center" Width="60" HorizontalAlignment="Right" FontSize="10" Foreground="#34495E"/>
                                            <TextBlock Text="{Binding IdsObjetoAsString}" FontSize="16" VerticalAlignment="Center" Foreground="Black" Margin="10,0"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>-->
    </Grid>
</UserControl>
