<UserControl x:Class="ExamenFinal.View.VentanaDesplegableView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:ExamenFinal.View" 
             xmlns:viewmodel="clr-namespace:ExamenFinal.ViewModel" 
             d:DataContext="{d:DesignInstance Type=viewmodel:VentanaDesplegableViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="White">
    <Grid>
        <!-- Define two columns -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        
        <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="10">
            <StackPanel Orientation="Vertical">
                <TextBlock Text="{Binding Objeto.Id, Mode=TwoWay, StringFormat='Id: {0}'}" Margin="0,0,0,10"/>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <Label Content="Usuario:" VerticalAlignment="Center"/>
                    <TextBox Text="{Binding Objeto.Usuario, Mode=TwoWay}" Width="200" Margin="5,0,0,0"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <Label Content="Productos:" VerticalAlignment="Center"/>
                    <TextBox Text="{Binding Objeto.Productos, Mode=TwoWay}" Width="200" Margin="5,0,0,0"/>
                </StackPanel>

                <TextBlock Text="{Binding Objeto.Fecha, Mode=TwoWay, StringFormat='CreatedDate: {0}'}" Margin="0,0,0,10"/>

            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Column="1" Orientation="Vertical" HorizontalAlignment="Right" Margin="10">
            <!--<Button Content="Update" Margin="10" Command="{Binding UpdateCommand}" Background="ForestGreen" Foreground="White"/>-->
            <Button Content="Add" Margin="10" Command="{Binding AddCommand}" Background="BlueViolet" Foreground="White"/>
            <Button Content="Close" Margin="10" Background="DeepSkyBlue" Foreground="White"
                    Command="{Binding DataContext.CloseCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
            <Button Content="Delete" Margin="10" Command="{Binding DeleteCommand}" Background="Red" Foreground="White"/>
        </StackPanel>
    </Grid>
</UserControl>
