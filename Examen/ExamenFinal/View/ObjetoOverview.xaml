<UserControl x:Class="ExamenFinal.View.ObjetoOverview"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ExamenFinal.View" 
             xmlns:viewmodel="clr-namespace:ExamenFinal.ViewModel" 
             xmlns:view="clr-namespace:ExamenFinal.View"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml" 
             d:DataContext="{d:DesignInstance Type=viewmodel:ObjetoOverviewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="White">
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type viewmodel:VentanaDesplegableViewModel}">
            <view:VentanaDesplegableView/>
        </DataTemplate>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="7*"/>
        </Grid.RowDefinitions>

        <!-- Barra de búsqueda -->
        <!--<StackPanel Orientation="Horizontal" Grid.Row="0" Margin="10">
            <TextBox Width="200" Height="30" Margin="0,0,10,0" 
                     Text="{Binding SearchId}" />
            <Button Content="Buscar" Width="80" Height="30" 
                    Command="{Binding SearchCommand}" 
                    VerticalAlignment="Center" />
        </StackPanel>-->

        <!-- Lista de elementos -->
        <ScrollViewer Grid.Row="1">
            <StackPanel>
                <ItemsControl ItemsSource="{Binding Items}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="5">
                                <StackPanel>
                                    <Button Height="100" Padding="5" 
                                            Command="{Binding DataContext.SelectViewModelCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                            CommandParameter="{Binding Id}">
                                        <Border Background="White" Height="50">
                                            <TextBlock Text="{Binding Nombre}" 
                                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </Button>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>

        <!-- Vista seleccionada -->
        <ContentControl Grid.Row="2" Content="{Binding SelectedViewModel}"/>
    </Grid>
</UserControl>
