<Window x:Class="ExamenFinal.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ExamenFinal"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml" 
        xmlns:System="clr-namespace:System;assembly=mscorlib"
        xmlns:viewmodel="clr-namespace:ExamenFinal.ViewModel" 
        xmlns:view="clr-namespace:ExamenFinal.View"
        d:DataContext="{d:DesignInstance Type=viewmodel:MainViewModel}"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <DataTemplate DataType="{x:Type viewmodel:LoginViewModel}">
            <view:LoginView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodel:RegistroViewModel}">
            <view:RegistroView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodel:PrincipalViewModel}">
            <view:PrincipalView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodel:GridViewModel}">
            <view:GridView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodel:ObjetoOverviewModel}">
            <view:ObjetoOverview/>
        </DataTemplate>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter"/>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Menu Bar 0-->
        <Menu FontSize="15" Grid.Row="0" HorizontalAlignment="Center" Margin="5" Background="#2C3E50"
                        Visibility="{Binding IsMenuVisible, Converter={StaticResource BoolToVisConverter}}">
            <MenuItem Header="_Productos" Foreground="White"
          Command="{Binding SelectViewModelCommand}"
          CommandParameter="{Binding DataContext.PrincipalViewModel, RelativeSource={RelativeSource AncestorType=Window}}"/>
            <MenuItem Header="_Pedidos" Foreground="White"
          Command="{Binding SelectViewModelCommand}"
          CommandParameter="{Binding DataContext.GridViewModel, RelativeSource={RelativeSource AncestorType=Window}}"/>
            <MenuItem Header="_Productos2" Foreground="White"
          Command="{Binding SelectViewModelCommand}"
          CommandParameter="{Binding DataContext.ObjetoOverviewModel, RelativeSource={RelativeSource AncestorType=Window}}"/>
            <MenuItem Header="_Logout" Foreground="White"
          Command="{Binding SelectViewModelCommand}"
          CommandParameter="{Binding DataContext.LoginViewModel, RelativeSource={RelativeSource AncestorType=Window}}"/>

        </Menu>


        <ContentControl Grid.Row="1" Content="{Binding SelectedViewModel}"/>

    </Grid>
</Window>