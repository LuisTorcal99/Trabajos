<UserControl x:Class="ExamenFinal.Controls.SidebarMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:ExamenFinal.Controls"
             xmlns:viewmodel="clr-namespace:ExamenFinal.ViewModel"
             mc:Ignorable="d" 
             Height="Auto" Width="120">

    <Grid Background="#2C3E50">
        <StackPanel Orientation="Vertical" HorizontalAlignment="Left">

            <Button Content="🏠 Inicio" Width="110" Height="50" Margin="5"
                    Background="Transparent" Foreground="White" FontSize="16" 
                    Command="{Binding InicioCommand}"/>

            <Button Content="📋 Grid" Width="110" Height="50" Margin="5"
                    Background="Transparent" Foreground="White" FontSize="16" 
                    Command="{Binding GridCommand}"/>

            <Button Content="🚪 Logout" Width="110" Height="50" Margin="5"
                    Background="Transparent" Foreground="White" FontSize="16" 
                    Command="{Binding LogoutCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>